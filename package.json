{
  "name": "model-engine-bb-plugin",
  "version": "0.3.0",
  "description": "A ModelEngine addon for Blockbench",
  "scripts": {
    "build": "gulp concat"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-concat": "^2.6.0"
  }
}
